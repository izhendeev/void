# Base Mini App

Мини-приложение для Base App, созданное с использованием Next.js и Base MiniApp SDK.

## 📋 Требования

- Node.js 18+ 
- npm, pnpm или yarn
- Аккаунт Base App
- Аккаунт GitHub
- Аккаунт Vercel (для деплоя)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env.local`:

```env
NEXT_PUBLIC_URL=https://your-app.vercel.app
```

⚠️ **Важно**: Укажите свой URL после деплоя на Vercel.

### 3. Запуск локально

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## 📦 Деплой на GitHub и Vercel

### Шаг 1: Загрузить в GitHub

1. Создайте новый репозиторий на GitHub (например: `base-miniapp`)

2. Инициализируйте git и загрузите код:

```bash
git init
git add .
git commit -m "Initial commit: Base Mini App"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/base-miniapp.git
git push -u origin main
```

### Шаг 2: Подключить к Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите "New Project"
3. Импортируйте ваш GitHub репозиторий
4. Добавьте переменную окружения:
   - **Name**: `NEXT_PUBLIC_URL`
   - **Value**: `https://your-project.vercel.app` (Vercel присвоит URL автоматически)
5. Нажмите "Deploy"

### Шаг 3: Обновить URL в коде

После деплоя Vercel присвоит вашему проекту URL (например: `base-miniapp.vercel.app`).

1. Обновите переменную `NEXT_PUBLIC_URL` в настройках Vercel (Settings → Environment Variables)
2. Передеплойте проект (или Vercel передеплоит автоматически)

## 🔐 Создание Account Association

### Шаг 4: Отключить Deployment Protection в Vercel

1. Зайдите в Settings → Deployment Protection
2. Отключите "Vercel Authentication" (переключите в OFF)
3. Сохраните изменения

### Шаг 5: Сгенерировать Account Association

1. Убедитесь, что ваш проект задеплоен и manifest доступен по адресу:
   `https://your-app.vercel.app/.well-known/farcaster.json`

2. Перейдите на [Base Build Account Association](https://base.build/account-association)

3. Вставьте URL вашего приложения (например: `base-miniapp.vercel.app`)

4. Нажмите "Verify" и следуйте инструкциям

5. Скопируйте сгенерированный объект `accountAssociation`

6. Обновите файл `app/.well-known/farcaster.json/route.ts`, заменив пустые поля:

```typescript
accountAssociation: {
  header: "ваш_header",
  payload: "ваш_payload", 
  signature: "ваш_signature"
}
```

7. Загрузите изменения в GitHub:

```bash
git add .
git commit -m "Add account association"
git push
```

Vercel автоматически передеплоит проект.

## ✅ Проверка приложения

1. Перейдите на [Base Build Preview](https://base.build/preview)

2. Вставьте URL вашего приложения

3. Проверьте:
   - ✅ "Metadata" - метаданные из manifest
   - ✅ "Account association" - правильно ли настроена ассоциация
   - ✅ Запуск приложения через кнопку "Launch"

## 📝 Структура проекта

```
base-miniapp/
├── app/
│   ├── .well-known/
│   │   └── farcaster.json/route.ts  # Manifest файл
│   ├── api/
│   │   └── webhook/route.ts         # Webhook endpoint
│   ├── layout.tsx                   # Root layout с metadata
│   ├── page.tsx                     # Главная страница
│   └── globals.css                  # Глобальные стили
├── public/                          # Статические файлы (иконки, изображения)
├── package.json
├── tsconfig.json
└── next.config.js
```

## 🖼️ Необходимые изображения

Добавьте в папку `public/` следующие изображения:

- `icon.png` - иконка приложения (рекомендуется 512x512px)
- `splash.png` - изображение для splash screen
- `hero.png` - главное изображение
- `og-image.png` - изображение для социальных сетей
- `screenshot1.png`, `screenshot2.png` - скриншоты приложения

## 📚 Документация

- [Base Mini Apps Documentation](https://docs.base.org/mini-apps/quickstart/create-new-miniapp)
- [Base MiniApp SDK](https://www.npmjs.com/package/@farcaster/miniapp-sdk)

## 🎯 Следующие шаги

1. Настройте manifest под ваше приложение
2. Добавьте нужные изображения
3. Реализуйте функциональность вашего приложения
4. Сгенерируйте Account Association через Base Build
5. Опубликуйте приложение, создав пост в Base App с URL вашего приложения
