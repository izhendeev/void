# Деплой смарт-контракта GameLeaderboard

## 📋 Требования

- Node.js 18+
- Аккаунт с ETH на Base (для оплаты газа)
- Приватный ключ кошелька (для деплоя)

## 🚀 Шаги деплоя

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта (скопируйте из `.env.example`):

```env
PRIVATE_KEY=your_private_key_here
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
BASE_MAINNET_RPC_URL=https://mainnet.base.org
```

⚠️ **ВАЖНО**: 
- Никогда не коммитьте файл `.env` в git!
- Приватный ключ должен быть от кошелька с ETH для оплаты газа
- Приватный ключ можно указывать с префиксом `0x` или без него

#### Получение RPC URL для Base:

- **Base Mainnet**: `https://mainnet.base.org` (публичный) или используйте [Alchemy](https://www.alchemy.com/) / [Infura](https://www.infura.io/) для приватного RPC
- **Base Sepolia**: `https://sepolia.base.org` (публичный) или приватный RPC от провайдера

### 3. Компиляция контракта

```bash
npm run compile
```

### 4. Деплой на Base Sepolia (тестовая сеть)

Для тестирования сначала задеплойте на Base Sepolia:

```bash
npm run deploy:sepolia
```

**Если вы видите ошибку о PRIVATE_KEY:**
1. Убедитесь, что файл `.env` создан в корне проекта
2. Добавьте ваш приватный ключ: `PRIVATE_KEY=your_key_here`
3. Убедитесь, что у вас есть тестовый ETH на Base Sepolia:
   - Получите тестовый ETH: https://www.coinbase.com/faucets/base-ethereum-goerli-faucet

После деплоя скопируйте адрес контракта из вывода, например:
```
✅ GameLeaderboard deployed to: 0x1234567890abcdef1234567890abcdef12345678
```

### 5. Деплой на Base Mainnet

После тестирования на Sepolia, задеплойте на mainnet:

```bash
npm run deploy:base
```

### 6. Обновление адреса контракта

После деплоя обновите адрес контракта в `.env.local`:

```env
NEXT_PUBLIC_CONTRACT_ADDRESS=0x...ваш_адрес_контракта
```

Адрес автоматически подхватится из переменной окружения в `app/lib/blockchain.ts`.

### 7. Получение тестового ETH для Base Sepolia

Если нужно протестировать на Sepolia, получите тестовый ETH через:
- [Base Sepolia Faucet](https://www.coinbase.com/faucets/base-ethereum-goerli-faucet)

## 🔒 Безопасность контракта

Контракт разработан с учетом требований Base:

- ✅ Максимально простой (только сохранение рекорда)
- ✅ Максимально безопасный (минимальный код = меньше багов)
- ✅ Защита от манипуляций (каждый игрок может обновить только свой рекорд)
- ✅ Низкая стоимость газа (оптимизирован)
- ✅ Простая структура данных (только mapping)
- ✅ Нет административных функций
- ✅ Нет возможности удаления данных

## 📝 Функции контракта

- `saveRecord(uint256 score)` - Сохранить рекорд (только если он лучше предыдущего)
- `getMyRecord()` - Получить свой рекорд
- `getRecord(address player)` - Получить рекорд игрока

## 📊 Структура данных

- **PlayerRecord**: `{bestScore: uint256, timestamp: uint256}`
- Контракт хранит только индивидуальные рекорды игроков
- Нет общего топ-листа (максимально простой и безопасный подход)

## ✅ Проверка для модерации Base

Контракт соответствует требованиям:
- Простой и понятный код
- Только необходимый функционал
- Безопасная реализация
- Низкая стоимость транзакций
- Нет административного доступа
- Иммутабельность данных (защита от манипуляций)

## 🔍 Проверка контракта на Basescan

После деплоя вы можете проверить контракт на:
- **Base Sepolia**: https://sepolia.basescan.org
- **Base Mainnet**: https://basescan.org

Вставьте адрес контракта в поиск, чтобы увидеть все транзакции и вызовы функций.

## 💡 Важные замечания

1. **Gas fees**: Каждая транзакция `saveRecord` требует оплаты газа. Убедитесь, что пользователи понимают это.

2. **Чтение данных**: Функции чтения (`getRecord`, `getMyRecord`) не требуют газа и могут вызываться бесплатно через RPC провайдера.

3. **Transaction sponsoring**: Base поддерживает sponsored transactions (Paymaster API). Можно настроить, чтобы транзакции оплачивал сам контракт или приложение, а не пользователи.

4. **Простота**: Контракт максимально простой - только сохранение и чтение рекордов. Это делает его безопасным и дешевым в использовании.
