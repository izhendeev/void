# 🚀 Инструкция: Загрузка проекта на GitHub

## ✅ Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на https://github.com/izhendeev
2. Нажмите кнопку **"New"** или **"+"** → **"New repository"**
3. Укажите имя репозитория (например: `void3-game` или `base-miniapp`)
4. **НЕ** создавайте README, .gitignore или license (уже есть в проекте)
5. Выберите **Public** или **Private** (по вашему желанию)
6. Нажмите **"Create repository"**

## 📦 Шаг 2: Подключение к GitHub

Выполните команды в терминале:

```bash
cd C:\Users\sense\base-miniapp

# Добавляем все файлы (кроме .env - он в .gitignore)
git add .

# Делаем первый коммит
git commit -m "Initial commit: VOID³ - Space Dodge Game"

# Переименуем ветку в main (если нужно)
git branch -M main

# Добавляем remote (замените REPO_NAME на имя вашего репозитория)
git remote add origin https://github.com/izhendeev/REPO_NAME.git

# Пушим код
git push -u origin main
```

**Важно:** Замените `REPO_NAME` на реальное имя вашего репозитория!

## 🔐 Безопасность

- ✅ Файл `.env` уже в `.gitignore` - приватный ключ не попадет в git
- ✅ В репозитории будут только публичные файлы
- ✅ Приватный ключ остается только локально
