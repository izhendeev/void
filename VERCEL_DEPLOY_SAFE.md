# 🔐 Безопасный деплой на Vercel

## ✅ Приватный ключ БЕЗОПАСЕН!

**Важно понимать:**
- ✅ Приватный ключ остается **ТОЛЬКО** на вашем компьютере
- ✅ Файлы `.env` и `.env.local` уже в `.gitignore` - они НЕ попадут в git
- ✅ Приватный ключ **НЕ нужен** в Vercel
- ✅ В Vercel нужны только **публичные** переменные окружения

## 📋 Что нужно сделать

### Шаг 1: Проверка безопасности

Убедитесь, что `.env` не попадет в git:

```bash
# Проверьте что .env в .gitignore (уже есть ✅)
cat .gitignore | grep .env
```

### Шаг 2: Инициализация Git (если еще не сделано)

```bash
cd C:\Users\sense\base-miniapp
git init
git add .
git commit -m "Initial commit: VOID³ game with smart contract"
```

**⚠️ ВАЖНО**: При `git add .` система автоматически проигнорирует `.env` файлы благодаря `.gitignore`

### Шаг 3: Создание GitHub репозитория

1. Зайдите на [github.com](https://github.com)
2. Создайте новый репозиторий (например: `void3-game`)
3. **НЕ** создавайте README или .gitignore (уже есть)
4. Подключите репозиторий:

```bash
git remote add origin https://github.com/YOUR_USERNAME/void3-game.git
git branch -M main
git push -u origin main
```

### Шаг 4: Деплой на Vercel

1. Зайдите на [vercel.com](https://vercel.com) и войдите через GitHub
2. Нажмите **"Add New Project"**
3. Импортируйте репозиторий `void3-game`
4. Настройки:
   - Framework: Next.js (автоматически)
   - Root Directory: `./`
   - Build Command: `npm run build` (по умолчанию)

### Шаг 5: Добавление переменных окружения в Vercel

**⚠️ НЕ добавляйте PRIVATE_KEY!** Он нужен только локально.

Добавьте только эти переменные:

1. **NEXT_PUBLIC_CONTRACT_ADDRESS**:
   - Value: `0x23c559E86635844eAf0ed63d95e081C7dB63b0b0`
   - Это адрес контракта на Base Sepolia (публичная информация)

2. **NEXT_PUBLIC_URL** (опционально, будет обновлен после деплоя):
   - Value: `https://your-app.vercel.app` (Vercel создаст URL автоматически)

**Как добавить:**
- В настройках проекта: **Settings** → **Environment Variables**
- Добавьте переменные для всех сред (Production, Preview, Development)
- Нажмите **"Deploy"**

### Шаг 6: Первый деплой

После деплоя:
1. Скопируйте URL вашего приложения (например: `void3-game.vercel.app`)
2. Обновите `NEXT_PUBLIC_URL` в Vercel на этот URL
3. Передеплойте проект

## 🔒 Безопасность: Что остается локально

**Эти файлы НЕ попадут в git/Vercel:**
- ✅ `.env` - содержит PRIVATE_KEY (нужен только для деплоя контракта)
- ✅ `.env.local` - содержит локальные настройки

**Что попадет в git/Vercel:**
- ✅ Код приложения
- ✅ Адрес контракта (это публичная информация)
- ✅ Все остальные файлы проекта

## 📝 Что используется где

### Локально (на вашем компьютере):
- `PRIVATE_KEY` - для деплоя контракта (`npm run deploy:sepolia`)
- `.env` - локальные настройки Hardhat

### В Vercel (публично):
- `NEXT_PUBLIC_CONTRACT_ADDRESS` - адрес контракта (публичная информация)
- `NEXT_PUBLIC_URL` - URL вашего приложения

### В Base App (для пользователей):
- Пользователи подключают свои кошельки через Base App SDK
- Приватный ключ пользователя остается на их устройстве
- Ваш приватный ключ НЕ используется в приложении

## ✅ Проверка безопасности

После деплоя проверьте, что приватный ключ не утек:

1. **Проверьте GitHub**:
   - Зайдите в ваш репозиторий на GitHub
   - Убедитесь, что файла `.env` нет в списке файлов

2. **Проверьте Vercel**:
   - Settings → Environment Variables
   - Убедитесь, что `PRIVATE_KEY` там нет
   - Должны быть только `NEXT_PUBLIC_*` переменные

3. **Проверьте код**:
   - В коде нет упоминаний приватного ключа
   - Код использует только `NEXT_PUBLIC_CONTRACT_ADDRESS`

## 🎯 Итог

- ✅ Приватный ключ остается только локально
- ✅ В git/Vercel попадают только публичные данные
- ✅ Приложение безопасно для деплоя
